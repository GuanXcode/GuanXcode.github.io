---
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import type { SessionUser } from '../lib/auth';
import WorkCard from './WorkCard.astro';
import SectionIcon from './SectionIcon.astro';

interface Props {
  user?: SessionUser;
  publicWorks: CollectionEntry<'works'>[];
  recentPosts: CollectionEntry<'blog'>[];
  typeLabels: Record<string, string>;
  privateWorkCount: number;
}

const { user, publicWorks, recentPosts, typeLabels, privateWorkCount } = Astro.props;

const profiles = await getCollection('profile');
const profile = profiles[0]?.data;

const role = profile?.role ?? 'AI Agent 与知识系统构建者';
const tagline = profile?.tagline ?? '以 Agent、RAG 与评测闭环，构建可生产的 AI 系统';
const publicBio =
  profile?.publicBio?.trim() ??
  '从场景切入：编排智能体、治理知识、用评测与护栏承接不确定性';

const skills = [
  {
    title: 'Agent 系统',
    summary: '把模型能力收成可编排、可中断、可交付的任务闭环',
    icon: 'timeline' as const,
    items: ['多 Agent 协作', '工具调用与 MCP', '任务状态机', '人机协同 / HITL'],
  },
  {
    title: '知识与 RAG',
    summary: '让回答可溯源、可权限隔离，而不是黑盒问答',
    icon: 'log' as const,
    items: ['多源检索链路', '权限与隔离', '召回评估', '知识持续更新'],
  },
  {
    title: '评测与护栏',
    summary: '用度量与约束承接模型波动，保证上线可控',
    icon: 'skills' as const,
    items: ['离线 / 在线评测', '幻觉与越权护栏', '成本与延迟治理', '可观测与回放'],
  },
  {
    title: 'AI 原生架构',
    summary: '以业务域与演进边界设计平台，而不是堆模型与中间件',
    icon: 'projects' as const,
    items: ['场景化落地', '领域建模', '平台化复用', '长期演进'],
  },
];

const heroTags = skills.map((s) => s.title);

function formatPostDate(date: Date) {
  return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
}
---

<div class="home-landing">
  <section class="home-hero-stage" aria-label="首页主视觉">
    <canvas class="home-hero-canvas" id="home-starfield" aria-hidden="true"></canvas>
    <div class="home-hero-stars home-hero-stars--far" aria-hidden="true"></div>
    <div class="home-hero-stars home-hero-stars--near" aria-hidden="true"></div>
    <div class="home-hero-grid" aria-hidden="true"></div>
    <div class="home-hero-glow" aria-hidden="true"></div>

    <div class="home-hero-inner">
      <p class="home-hero-hello">你好，我是</p>
      <h1 class="home-hero-brand">AI Kosei</h1>
      <p class="home-hero-role">{role}</p>
      <p class="home-hero-lead">{tagline}</p>
      {publicBio && <p class="home-hero-desc">{publicBio}</p>}

      <div class="home-hero-ctas">
        <a href="/me/" class="home-cta home-cta--primary">
          {user ? '进入履历' : '查看履历'}
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14M13 6l6 6-6 6" />
          </svg>
        </a>
        <a href="/research/" class="home-cta home-cta--ghost">阅读调研</a>
        <a href="/projects/" class="home-cta home-cta--ghost">查看项目</a>
      </div>

      {heroTags.length > 0 && (
        <div class="home-hero-tags">
          {heroTags.map((area) => (
            <span class="home-hero-tag">{area}</span>
          ))}
        </div>
      )}
    </div>
  </section>

  <section class="home-section" id="skills">
    <div class="home-section-inner">
      <header class="home-section-head">
        <h2 class="home-section-title">AI Native 能力</h2>
        <p class="home-section-sub">面向可上线交付的 Agent、知识与评测能力</p>
      </header>
      <div class="home-skill-grid">
        {skills.map((group) => (
          <article class="home-skill-card">
            <div class="home-skill-card-top">
              <span class="home-skill-icon" aria-hidden="true">
                <SectionIcon name={group.icon} class="w-4 h-4" />
              </span>
              <div>
                <h3 class="home-skill-title">{group.title}</h3>
                <p class="home-skill-summary">{group.summary}</p>
              </div>
            </div>
            <div class="home-skill-chips">
              {group.items.map((item) => (
                <span class="home-skill-chip">{item}</span>
              ))}
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="home-section" id="projects">
    <div class="home-section-inner">
      <header class="home-section-head home-section-head--row">
        <div>
          <h2 class="home-section-title">精选作品</h2>
          <p class="home-section-sub">公开可浏览的项目与作品</p>
        </div>
        <a href="/projects/" class="home-section-more">
          全部项目
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14M13 6l6 6-6 6" />
          </svg>
        </a>
      </header>
      {privateWorkCount > 0 && (
        <p class="home-section-note">
          另有 {privateWorkCount} 项需 <a href="/login?next=/projects/">授权查看</a>
        </p>
      )}
      {publicWorks.length > 0 ? (
        <div class="home-works-grid">
          {publicWorks.map((work) => (
            <WorkCard
              title={work.data.title}
              description={work.data.description}
              type={typeLabels[work.data.type] ?? work.data.type}
              typeKey={work.data.type}
              slug={work.id}
              url={work.data.url}
              publicBadge={work.data.visibility === 'public'}
            />
          ))}
        </div>
      ) : (
        <p class="home-empty">暂无公开展示的作品</p>
      )}
    </div>
  </section>

  <section class="home-section home-section--last" id="research">
    <div class="home-section-inner">
      <header class="home-section-head home-section-head--row">
        <div>
          <h2 class="home-section-title">近期调研</h2>
          <p class="home-section-sub">工程笔记、方法与复盘</p>
        </div>
        <a href="/research/" class="home-section-more">
          全部调研
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14M13 6l6 6-6 6" />
          </svg>
        </a>
      </header>
      {recentPosts.length > 0 ? (
        <div class="home-posts">
          {recentPosts.map((post) => (
            <a href={`/blog/${post.id}/`} class="home-post-card">
              <time class="home-post-date">{formatPostDate(post.data.date)}</time>
              <h3 class="home-post-title">{post.data.title}</h3>
              <p class="home-post-excerpt">
                {post.data.subtitle || post.body?.replace(/<[^>]*>/g, '').replace(/[#*>`\[\]()!]/g, '').trim().slice(0, 110)}
              </p>
              {post.data.tags?.length > 0 && (
                <div class="home-post-tags">
                  {post.data.tags.slice(0, 3).map((tag) => (
                    <span class="home-post-tag">{tag}</span>
                  ))}
                </div>
              )}
              <span class="home-post-read">阅读文章 →</span>
            </a>
          ))}
        </div>
      ) : (
        <p class="home-empty">暂无调研笔记</p>
      )}
    </div>
  </section>
</div>

<script>
  function initHomeStarfield() {
    const canvas = document.getElementById('home-starfield');
    if (!(canvas instanceof HTMLCanvasElement)) return;
    if (canvas.dataset.ready === '1') return;
    canvas.dataset.ready = '1';

    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    const stage = canvas.parentElement;
    if (!stage) return;

    /** @type {{ x: number; y: number; r: number; vx: number; vy: number; a: number; tw: number; sp: number }[]} */
    let stars = [];
    let raf = 0;
    let running = false;

    function resize() {
      const rect = stage.getBoundingClientRect();
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      canvas.width = Math.max(1, Math.floor(rect.width * dpr));
      canvas.height = Math.max(1, Math.floor(rect.height * dpr));
      canvas.style.width = `${rect.width}px`;
      canvas.style.height = `${rect.height}px`;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      seed(rect.width, rect.height);
    }

    function seed(w, h) {
      const count = Math.min(110, Math.max(48, Math.floor((w * h) / 14000)));
      stars = Array.from({ length: count }, () => ({
        x: Math.random() * w,
        y: Math.random() * h,
        r: Math.random() * 1.4 + 0.4,
        vx: (Math.random() - 0.5) * 0.12,
        vy: -0.04 - Math.random() * 0.1,
        a: 0.25 + Math.random() * 0.7,
        tw: Math.random() * Math.PI * 2,
        sp: 0.8 + Math.random() * 1.6,
      }));
    }

    function drawStatic() {
      const w = canvas.clientWidth;
      const h = canvas.clientHeight;
      ctx.clearRect(0, 0, w, h);
      for (const s of stars) {
        ctx.beginPath();
        ctx.fillStyle = `rgba(125, 211, 252, ${s.a * 0.85})`;
        ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
        ctx.fill();
      }
    }

    function tick() {
      if (!running) return;
      const w = canvas.clientWidth;
      const h = canvas.clientHeight;
      ctx.clearRect(0, 0, w, h);

      for (const s of stars) {
        s.x += s.vx;
        s.y += s.vy;
        s.tw += 0.02 * s.sp;
        if (s.y < -4) {
          s.y = h + 4;
          s.x = Math.random() * w;
        }
        if (s.x < -4) s.x = w + 4;
        if (s.x > w + 4) s.x = -4;

        const pulse = 0.45 + 0.55 * Math.abs(Math.sin(s.tw));
        const alpha = s.a * pulse;
        ctx.beginPath();
        ctx.fillStyle = `rgba(186, 230, 253, ${alpha})`;
        ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
        ctx.fill();

        if (s.r > 1.1) {
          ctx.beginPath();
          ctx.fillStyle = `rgba(56, 189, 248, ${alpha * 0.35})`;
          ctx.arc(s.x, s.y, s.r * 2.2, 0, Math.PI * 2);
          ctx.fill();
        }
      }

      raf = requestAnimationFrame(tick);
    }

    function start() {
      if (running) return;
      running = true;
      raf = requestAnimationFrame(tick);
    }

    function stop() {
      running = false;
      cancelAnimationFrame(raf);
    }

    resize();
    if (reduceMotion) {
      drawStatic();
    } else {
      start();
    }

    const onResize = () => {
      resize();
      if (reduceMotion) drawStatic();
    };
    window.addEventListener('resize', onResize);

    const onVisibility = () => {
      if (reduceMotion) return;
      if (document.hidden) stop();
      else start();
    };
    document.addEventListener('visibilitychange', onVisibility);
  }

  initHomeStarfield();
  document.addEventListener('astro:page-load', initHomeStarfield);
</script>
